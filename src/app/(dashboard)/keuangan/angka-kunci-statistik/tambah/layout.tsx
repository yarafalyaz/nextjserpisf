import type { Metadata } from "next"

export const metadata: Metadata = { title: "Tambah Angka Kunci" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
