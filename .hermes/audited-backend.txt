2026-06-15 | hrm.actions.ts (getPayrollEstimation) | Collapsed 5 sequential DB queries (employee+loans, overtimes, appreciations, latePenalty, attendanceSummary) into a single Promise.all. Material impact in generateBulkPayroll, which calls this estimator once per active employee inside a serial loop: cuts 5×N sequential round-trips to N batched round-trips. Tsc clean, 2484/2484 tests pass. Commit ec4a005.
2026-06-15 | hrm.actions.ts (generateBulkPayroll) | Hoisted the per-employee N+1 into getBulkPayrollEstimations: single Promise.all fans out 9 read queries (settings, workSchedules, holidays, departmentHolidays, employees+loans, overtimes, appreciations, attendances, leaves) regardless of N, replaces 5×N round-trips. Collapsed N prisma.payroll.create into 1 prisma.payroll.createMany({ skipDuplicates: true }) — P2002 race semantics preserved. Adapted hrm.actions.test.ts mocks (added createMany mock and updated findMany shape). Tsc clean, 2488/2488 tests pass. Commit d932d1a.
2026-06-15 | goods-receipt.hook.ts (onGoodsReceiptVerified) | Eliminated N+1 item meta and UoM factor lookups inside the stock move creation loop. Pre-fetched metadata and UoM conversions in two queries outside the loop, dropping the DB round-trips from O(N) to O(1) for goods receipt completion. Tsc clean, 2492/2492 tests pass.
2026-06-15 | sales-return.hook.ts (onSalesReturnCompleted) | Eliminated N+1 default warehouse lookups. Replaced per-item findUnique with a single bulk findMany hoisted before the stock-move loop.
2026-06-15 | hrm.actions.ts (generateBulkPayroll) + document-number.ts + document-sequence.service.ts | Eliminated the last N+1 in bulk payroll: generateDocumentNumber("PAYROLL") was called serially once per employee (N sequence-counter bumps = N floor-scan findMany + N $transaction round-trips). Added DocumentSequenceService.nextBatch(key, count, floor) — reserves a contiguous block of sequence numbers in ONE atomic upsert (GREATEST(current_value + count, floor+count) semantics, same anti-collision floor as next()), and generateDocumentNumberBatch() to format the block. generateBulkPayroll now does 1 batched call instead of N. Added 3 unit tests for nextBatch (empty/count=1 fallback/contiguous block math). Tsc clean, 2499/2499 tests pass.
2026-06-15 | self-attendance.actions.ts (selfCheckIn/selfCheckOut) | Fixed schedule-blind overtime and half_day classification. 1) selfCheckIn wrongly hardcoded Sunday as the only non-working day regardless of the employee's work schedule (Mon-Fri employees lost overtime on Saturday; Sunday-shift employees were wrongly forced to overtime on their scheduled Sunday). 2) selfCheckOut resolved the schedule using the check-OUT day, so an overnight shift (Mon 22:00 → Tue 06:00) checking out at Tue 06:00 was checked against Tuesday's day-shift schedule (ends 17:00) and incorrectly marked half_day. Refactored resolveScheduleInfo to return `hasSchedule` (respecting `workDays` using attendance-summary.service precedence) + `endTime`, guarded isOvertimeDay with `!hasSchedule`, and enforced checkout half_day to use the check-in day's schedule with overnight cross-midnight checkout awareness. Added 4 unit tests. Tsc clean, 2503/2503 pass.
2026-06-15 | sales.actions.ts (createQuotation) | Replaced serial per-section create + createMany loop with a single nested write on tx.quotation.create (sections.create -> items.create). For a quotation with S sections and I total items, DB round-trips drop from 1 + 2S to 1 (header + nested). Prisma serializes the nested graph into one batched INSERT batch under the hood. Tsc clean, 2503/2503 tests pass (updated sales-quotation.actions.test.ts mock to assert on the nested write payload). Commit 5030d5d.
2026-06-15 | purchase-return.hook.ts + work-order.hook.ts | Eliminated N+1 default warehouse lookups during stock-move creation loop. Hoisted a single batched item.findMany upfront before the item iteration. Drops DB round-trips from O(N) to O(1) per hook. Tsc clean, 2504/2504 tests pass.
2026-06-15 | hrm.actions.ts (updateOvertimeRequest) | Fixed integrity flaw where an already-approved or rejected overtime request could be silently edited. The action had no status guard and hardcoded `status: 'pending'` on update, allowing users with edit access to bypass the approval flow, change hours/dates after the fact, and silently revert the approval status without triggering a re-approval. Added a findUniqueOrThrow status guard mirroring updateLeaveRequest. Added a failing test which now passes. Tsc clean, 2505/2505 tests pass.
2026-06-15 | down-payment.hook.ts + inventory-transfer.hook.ts + material-issue.hook.ts + stock-adjustment.hook.ts | Replaced N+1 serial loops with batched queries. (1) down-payment.hook.ts: collapsed per-item product.findFirst + redundant productMaterial.findMany (materials already eager-loaded) + per-item item.findUnique into a single product.findMany (OR-contains) + single item.findMany (IN clause), with rendering driven by a Map keyed by itemId — drops 3N round-trips to 2. (2) inventory-transfer.hook.ts: collapsed the per-item stockMove.findFirst (cost-basis lookup) into a single stockMove.findMany (IN clause) keyed by itemId; captured the create return id to eliminate the post-create stockMove.findFirst({ documentNo }) round-trip — drops 2N round-trips to 0. (3) material-issue.hook.ts + stock-adjustment.hook.ts: collapsed per-item $queryRaw FOR UPDATE + per-item item.findUnique + per-item generateDocumentNumber("SM") into a single $queryRaw FOR UPDATE (IN clause) + single item.findMany (IN clause) + single generateDocumentNumberBatch — drops 3N round-trips to 3. Updated mocks in stock-adjustment-baseline.test.ts and inventory-transfer-aggregation.test.ts (added findMany / generateDocumentNumberBatch spies, capture ID from create). Tsc clean, 2505/2505 tests pass. Commit 08666a2.
2026-06-15 | goods-receipt.hook.ts + accounting.hook.ts | Eliminated N+1 serial `.create()` loops. (1) `goods-receipt.hook.ts`: Collapsed per-serial item.serialNumber loop into a single batched `itemSerial.createMany`. (2) `accounting.hook.ts`: Collapsed dynamic `journalEntry.create()` loop in `onPurchaseReturnProcessed` into a single `createMany` batch. Updated hooks and tests to support `createMany`. Tsc clean, 2508/2508 tests pass.
2026-06-15 | project.actions.ts (getProjectStageProgress) | Fixed permission leak (view-only users triggered a write): action previously called `initializeProjectStages` unconditionally, which checks `edit_projects` and threw 403. Refactored to only auto-initialize if user holds `edit_projects`, otherwise gracefully returns empty stage array. Added 2 regression tests checking read-only and edit-permission flows. Tsc clean, 2508/2508 tests pass.
2026-06-15 | project.actions.ts (getProjectProgress) | Eliminated redundant aggregate query: merged 2 independent `task.count()` lookups (total and completed) into a single `task.groupBy({ by: ['status'], _count: true })`, halving DB round-trips for the dashboard widget. Tsc clean, 2508/2508 tests pass.
2026-06-15 | purchase.actions.ts (deleteGoodsReceipt) | Fixed orphan ItemSerial leak: deleting a verified GoodsReceipt reversed stock moves, inventory layers, qty_on_hand, GR items, journal posting, and PO status, but left the ItemSerial rows registered by onGoodsReceiptVerified from the receipt's serialNumbers intact. ItemSerial has no FK back to GoodsReceipt, so the serials became orphans flagged 'available' — the next attempt to receive the same serials crashed on unique(serialNumber), and the inventory subledger silently disagreed with the GL/stock totals. Fix: in the delete tx, look up the GR's goodsReceiptItem.serialNumbers (the serials captured at verification), and tx.itemSerial.deleteMany({ where: { serialNumber: { in: registeredSerials }, status: 'available' } }) — scopes the delete to 'available' rows only so sold/used serials remain as the audit trail for the downstream sales invoice. Added 4 vitest cases (basic call, no-serial skip, multi-item flatten, blank/whitespace filter) using mock-isolated transaction; the original test fails on the old code with 0 calls to itemSerial.deleteMany and passes after the fix. Tsc clean, 2512/2512 tests pass. Commit 82445c6.
2026-06-15 | src/actions/purchase.actions.ts (confirmVendorPayment) + src/lib/hooks/accounting.hook.ts (onSalesInvoicePosted) | Collapsed N vendorPaymentAllocation.create inside confirmVendorPayment and 2-3 journalEntry.create calls inside onSalesInvoicePosted into atomic createMany queries, dropping O(N) database round-trips. Tsc clean, 2512/2512 tests pass. Commit 5946503.
2026-06-15 | hrm.actions.ts (updateEmployeeLoan) | Fixed edit guard bypass: a loan with status `paid_off` is a completed financial record that has already been fully amortized against past payroll runs. The edit action `updateEmployeeLoan` had no status guard and permitted arbitrary edits (mutating totalAmount and shifting remainingAmount back above 0). Because `markPayrollPaid` only touches `active` loans, this allowed edits to resurrect an uncollectible balance on a settled loan or silently mutate history without restarting amortization. Fix: added `existing.status !== "active"` integrity check mimicking its `updateLeaveRequest` sibling. Added 2 vitest cases (active vs non-active loan edits). Tsc clean, 2514/2514 tests pass.
2026-06-15 | expense.hook.ts (onExpenseApprovedSyncPettyCash) | Collapsed N+1 sequential tx.pettyCash.update loop into a single Promise.all batch. When approving a petty cash expense, the running balance chain is recalculated and enforced against negative balances — but earlier logic fired an update query serially per record in the chain. Hoisted the updates to an array and flushed them concurrently. All tests pass. Commit e9a0349.
2026-06-15 | accounting.hook.ts (onPettyCashCreated) | Fixed accounting period-lock bypass and GL dating corruption. The hook verified the period and dated the journal using `pettyCash.transactionDate ?? new Date()`, but the API writes the user-input date to `pettyCash.date` and leaves `transactionDate` null. This caused back-dated petty-cash entries to silently bypass the period lock and post to the GL with today's date, corrupting the GL invariant. Changed to check and use `pettyCash.transactionDate ?? pettyCash.date`. Added vitest regression case. Tsc clean, 2515/2515 tests pass.
2026-06-15 | inventory-fifo.ts (consumeFifoLayers) | Eliminated N+1 sequential update loops for inventory layers and item batches. Collapsed serial `tx.inventoryLayer.update` and `tx.itemBatch.update` inside loops into concurrent `Promise.all` batched execution, dropping O(N) database round-trips to O(1) concurrent latency when stock-out consumes across multiple FIFO layers or batch lots. Tsc clean, 2515/2515 tests pass.
2026-06-15 | hrm.actions.ts (getBulkPayrollEstimations) + bulk.actions.ts (bulkDelete) | Fixed IDOR / unauthenticated server-action exposure. (1) `getBulkPayrollEstimations` was declared `export async` inside a `"use server"` file, making it a callable network endpoint that returned salary/loan/attendance data for any `[employeeIds]` array with zero permission check. Renamed the computation to module-scoped `computeBulkPayrollEstimations` (no `export` → not a server action) and added a thin public wrapper that calls `await requirePermission("view_payroll")` before delegating. `generateBulkPayroll` (which already requires `create_payroll`) now calls the internal helper directly, skipping the redundant second perm check. (2) `bulkDelete`: tightened comment around the upfront `modelPermissionMap` lookup + `requirePermission` so future maintainers don't accidentally move the auth below the validation guards — the previous ordering was technically still safe (the allowlist was the first check after input validation) but the rationale wasn't obvious. Tsc clean, 2515/2515 tests pass. Commit b79f7c5.
2026-06-15 | inventory.actions.ts + inventory.schemas.ts | Fixed same-warehouse inventory transfer. Added schema refinement to prevent sourceWarehouseId === destinationWarehouseId. Previously, a same-warehouse transfer would slip through, consuming FIFO layers out of the warehouse, then onTransferReceived would create a fresh IN layer back into the SAME warehouse — collapsing multiple distinct-cost FIFO layers into one weighted-average layer and corrupting the cost basis for future stock-outs. Also fixed updateInventoryTransfer action bypassing Zod validation. Tsc clean, 2518/2518 tests pass. Commit.
2026-06-15 | work-order.hook.ts + purchase-return.hook.ts + inventory-transfer.hook.ts (onTransferProcessed, onTransferReceived) + sales-return.hook.ts | Collapsed the last per-item N+1 sequence-counter + item-FOR-UPDATE loops in stock-move hooks. Replaced N serial generateDocumentNumber("SM") calls with one generateDocumentNumberBatch (single atomic counter bump) and N serial `SELECT id FROM items WHERE id = $X FOR UPDATE` with one `IN (${Prisma.join(itemIds)}) FOR UPDATE`. Pattern now uniform across all stock-move hooks. Imported Prisma from @prisma/client. Updated inventory-transfer-aggregation.test.ts mock from generateDocumentNumber to generateDocumentNumberBatch. Tsc clean, 2518/2518 tests pass. Commit b078e42.
2026-06-16 | inventory-fifo.ts (consumeFifoLayers) | Hoisted nested itemBatch.findMany queries outside the consumption loop to avoid N+1 over distinct batch strings. Commit eeab4b4.
2026-06-16 | hrm.actions.ts (getPayrollEstimation) + confirmVendorPayment + goods-receipt.hook.ts | Fixed critical IDOR / auth-bypass in getPayrollEstimation: the previous signature had a wire-controllable skipPermissionCheck boolean; passing true forged a super_admin session and bypassed both requirePermission and the per-employee IDOR guard, exposing any employee's salary/loans/attendance. Refactored the computation to a module-scoped computePayrollEstimation(sessionUser) helper (no export → not a "use server" endpoint) and exposed a thin public wrapper that always calls requirePermission("view_payroll") and forwards the session to the IDOR check. Mirrors the getBulkPayrollEstimations hardening. Tightened the return-type union at the three call sites (payroll-form.tsx UI plus 2 hrm test cases) with a discriminated 'error' in result guard. (2) confirmVendorPayment (purchase.actions.ts): eliminated the serial N+1 vendorBill.update loop after the allocation createMany — wrap the per-allocation updates in a single Promise.all (allocations are guaranteed distinct bill ids by allocatePaymentToBills, so no write-write conflicts). (3) goods-receipt.hook.ts: hoisted generateDocumentNumber("SM") out of the per-item stock-move loop into a single generateDocumentNumberBatch("SM", count) call, and added the missing batched item row lock SELECT id FROM items WHERE id IN (...) FOR UPDATE (the per-item UPDATE items SET qty_on_hand serialised locks anyway, but doing it pre-loop matches the work-order.hook / purchase-return.hook / inventory-transfer.hook / sales-return.hook pattern and avoids acquiring row locks in loop order — dead-lock-proof). Updated goods-receipt-over-receipt.test.ts mocks (added generateDocumentNumberBatch spy that produces SM-N per line). Tsc clean, 2526/2526 tests pass. Commit 2b1da02.
2026-06-16 | src/app/api/cron/asset-depreciation/route.ts | Eliminated two N+1 patterns in the monthly asset-depreciation cron. (1) Replaced the per-asset assetHistory.findFirst("already depreciated this period?") check with a single pre-fetch: prisma.assetHistory.findMany({ assetId: { in: assets.map(a => a.id) }, ... }) + Set lookup, dropping N round-trips to 1. (2) Replaced the per-asset prisma.documentSequence.upsert("JOURNAL") with a single DocumentSequenceService.nextBatch("JOURNAL", toProcess.length) — same atomic-counter semantics as the rest of the GL, but N counter bumps → 1. (3) The remaining N per-asset $transactions now run concurrently via Promise.allSettled, isolating per-asset failures (errors stay in errorDetails, the rest of the batch still posts). Updated asset-depreciation route.test.ts mocks to match (assetHistory.findMany + DocumentSequenceService.nextBatch spies, added assertion that nextBatch is called with the eligible-asset count). Tsc clean, 2528/2528 tests pass. Commit 05d5f0c.
2026-06-16 | src/lib/hooks/accounting.hook.ts | Eliminated 26 sequential tx.journalEntry.create round-trips across 9 journal-posting hooks (onSalesPaymentCreated, onPurchaseOrderReceived, onStockAdjustmentProcessed x2 branches, onWorkOrderCompleted, onExpenseApproved, onPettyCashCreated x2 branches, onSalesReturnCompleted, onMaterialIssue). Replaced each pair/quad of separate create calls with one createMany batch after the journal header is created, dropping N DB round-trips to 1 per hook invocation. onPurchaseReturnProcessed already used createMany. Tsc clean, 2536/2536 tests pass. Commit 932ea76.
28|2026-06-16 | manufacturing.actions.ts (syncProjectStatus) | Fixed data-integrity bug in the local syncProjectStatus helper called from completeWorkOrder: `skipped` stages were not counted as terminal "done" states, so a project with any skipped stage would never auto-transition to `completed` even after the last WorkOrder completed. Mirrored the canonical fix in src/actions/project.actions.ts (lines 291-295 of that file). Also nulled `endDate` when transitioning a partially-completed project back to `in_progress` (the canonical helper also clears it on reactivation) — without this, a project could end up with a non-null endDate on a still-in_progress project, which would make the dashboard display misleading dates. Added 1 regression vitest case asserting that mixed completed+skipped stages flip the project to `completed`. Tsc clean, 2539/2539 tests pass. Commit 891b023.
29|2026-06-16 | master.actions.ts (deletePosition) | Fixed data-integrity bug: unguarded deletion of a position. Deleting a position with active employees would silently cascade a SetNull onto Employee.positionId, erasing their job-title assignments with no warning. Added an empCount validation mimicking deleteDepartment to refuse deletion and force reassignment first. Added 1 vitest case. Tsc clean, 2540/2540 tests pass. Commit 0087804.
30|2026-06-16 | finance.actions.ts (updateExpense + updatePettyCash) | Closed Zod validation bypass in finance update actions. Both createExpense and createPettyCash validate input via parseFormData(expenseSchema/pettyCashSchema), but the corresponding updateExpense and updatePettyCash hand-parsed formData via safeId/requireNumber/formData.get, bypassing the schema's positive-amount guard, IN/OUT enum, and max-string-length constraints. As a result, an authenticated editor could push a draft expense to a non-positive amount or set petty-cash type to an arbitrary string — both silently corrupting downstream chain recomputation or GL posting. Refactored both updates to call parseFormData(schema) and consume the parsed v.* fields (mirrors the create paths). Updated attachmentIds read to use v.attachmentIds for consistency. Added 2 regression vitest cases (negative expense amount, invalid petty-cash type) asserting the result.error contains "Validasi gagal" AND that the DB update was NOT called. Tsc clean, 2542/2542 tests pass. Commit 904d0a5.
src/actions/finance.actions.ts (fix: edit_budgets permission gate inconsistently used create_budgets)
32|2026-06-16 | src/lib/hooks/accounting.hook.ts (onSalesInvoicePosted) + src/actions/sales.actions.ts (updateDeliveryOrder) | Fixed N+1 in onSalesInvoicePosted COGS/stock-out loop. Previously, for each invoice line: (1) toBaseFactor() fired a fresh tx.item.findUnique + tx.uomConversion.findUnique pair; (2) a per-line tx.$queryRaw SELECT id FROM items FOR UPDATE. Hoisted both reads outside the loop: 1) bulk-loaded item meta (id, cost, isProduct, defaultWarehouseId, unitOfMeasure) once, 2) bulk-loaded uomConversion for the union of distinct (itemId, code) pairs, 3) pre-locked all item rows in a single SELECT id FROM items WHERE id IN (...) FOR UPDATE (matches the work-order / purchase-return / sales-return / inventory-transfer / goods-receipt pattern, deadlock-proof). Replaced the per-line `SM-INV-${invoice.documentNo}-${line.itemId}` ad-hoc string with a single generateDocumentNumberBatch("SM", productCount) call (non-product items excluded from the counter so service lines don't waste document numbers). The remaining stockMove.create and consumeFifoLayers stay in the loop because they have to interleave with qty_on_hand decrement and per-item FIFO math. Drops DB round-trips from O(N) to O(1) reads. Updated accounting-hook.test.ts mock (added uomConversion.findMany to the tx, mocked generateDocumentNumberBatch to produce ["SM-1","SM-2",...]). Separately, closed the Zod validation bypass in updateDeliveryOrder: the action hand-parsed formData via formData.get() and cast everything to string, bypassing the existing updateDeliveryOrderSchema's required salesOrderId, non-empty date, max-length guards — a draft editor could push arbitrary strings into date (crashing new Date()), non-numeric salesOrderId, or 5MB shipping notes. Refactored to call parseFormData(updateDeliveryOrderSchema) and consume v.* fields (mirrors createDeliveryOrder). Added 1 regression test (empty-date rejection asserts error contains "Validasi gagal" AND deliveryOrder.update was NOT called). Tsc clean, 2543/2543 tests pass. Commit 1f8fa6c.
2026-06-16 | src/actions/inventory.actions.ts (updateStockAdjustment + updateMaterialIssue) | Closed Zod validation bypass on two inventory update actions. Both hand-parsed formData via formData.get()/requireId/safeId/new Date(...), bypassing the stockAdjustmentSchema/materialIssueSchema guards that the create paths and the already-fixed updateInventoryTransfer use. Concrete impact: an authenticated editor could push (a) warehouseId=0 / non-existent / arbitrary ints (no schema min(1)), (b) an empty/garbage date crashing new Date() into Invalid Date, (c) 5MB notes blobs (no max-length), (d) arbitrary type/reason strings (no max-length / no enum). Refactored both to call parseFormData(stockAdjustmentSchema|materialIssueSchema) and consume v.* fields; v.notes/v.reason/v.type are coerced from undefined→null inside the update data. Added 6 vitest cases (warehouseId=0 reject, empty-date reject, oversized-notes reject × 2 actions) asserting res.error is a string AND the DB update was NOT called. Tsc clean, 2552/2552 tests pass. Commit 206b687.
2026-06-16 | src/actions/sales.actions.ts (updateSalesOrder/updateSalesInvoice/updateSalesPayment/updateSalesReturn/updateDeliveryOrder/updateDownPayment) + prisma/seed.ts | Fixed inconsistent permission-gate security bypass: 6 sales update actions checked create_* permissions instead of the edit_* permissions that the UI edit pages actually gate on. This allowed a user who only has create privileges to modify existing documents directly over the wire, bypassing edit restrictions. Also added the missing edit_sales_invoices/edit_sales_payments/edit_delivery_orders permissions to prisma/seed.ts. Added 6 regression assertions to sales.actions.test.ts. All tests pass.
2026-06-16 | src/actions/master.actions.ts (createWarehouse/updateWarehouse/createAccount/updateAccount) + src/lib/validations/schemas.ts | Closed Zod validation bypass in 4 master-data server actions. The form schemas in @/lib/validators validate the user input via react-hook-form, but the server actions hand-parsed formData (requireString / formData.get() as string / `as "ASSET" | "LIABILITY" | "EQUITY" | "REVENUE" | "EXPENSE"`) — bypassing the schema entirely. An authenticated editor with `create_warehouses` / `edit_warehouses` could push a warehouse with an empty name; an editor with `create_accounts` / `edit_accounts` could push an arbitrary account `type` string (e.g. `"REVENUE_BAD"`) past the action, and the balance sheet / income statement / trial balance all group by `type` so the bad row silently corrupted financial reporting. Fix: added `warehouseServerSchema` and `accountServerSchema` to `src/lib/validations/schemas.ts` (server-side variants with `z.coerce.number()` and an enum-validated `type`), threaded them through `parseFormData` in createWarehouse/updateWarehouse/createAccount/updateAccount, and switched the field reads from hand-parsed formData to `v.*`. Added 4 vitest regression assertions: createWarehouse + updateWarehouse reject empty name; createAccount + updateAccount reject invalid type. Fixed an existing test that was passing the invalid type string `"test"`. Tsc clean, 2556/2556 tests pass. Commit 4a056d6.
2026-06-16 | src/lib/services/inventory.service.ts (issueProjectMaterials) | Eliminated N+1 inside the project material-issue loop. The function was: (1) running a per-item `tx.stockMove.findFirst` idempotency check (N reads), (2) calling `generateDocumentNumber("SM")` serially once per item (N sequence-counter bumps = N floor-scan + N $transaction round-trips), and (3) looping the actual stock-move create + postMove. Replaced (1) with a single batched `tx.stockMove.findMany({ where: { itemId: { in: validItemIds }, referenceType: "project_material_issue", referenceId: projectId } })` + Map lookup, and replaced (2) with a single `generateDocumentNumberBatch("SM", itemsToIssue.length)` call (mirrors the work-order / purchase-return / sales-return / inventory-transfer / material-issue / stock-adjustment pattern). The stockMove.create + postMove stays in the loop because they must interleave with FIFO consumption + qty_on_hand decrement to maintain per-item invariants inside the same transaction. Updated inventory-service.test.ts: hoisted `generateDocumentNumberBatch` mock, switched `buildIssueTx` to expose `findMany` (returning `{id,itemId}[]`) instead of `findFirst`, updated the idempotent and combined-existing+new tests to assert the new shape. Tsc clean, 2557/2557 tests pass. Commit c1b285d.
src/actions/master.actions.ts
src/lib/validations/crm.schemas.ts
2026-06-16 | src/actions/sales.actions.ts (updateQuotation/updateSalesOrder/updateSalesInvoice/updateSalesPayment/updateSalesReturn/updateDownPayment) + src/lib/validations/sales.schemas.ts | Closed Zod validation bypass in 6 sales update server actions. The corresponding create paths validated via parseFormData(), but these updates hand-parsed formData (requireId / formData.get / etc), bypassing max-length, string-emptiness, negative-amount, and enum constraints. This allowed authenticated editors to push empty dates, 5MB+ string blobs into notes, or negative values that could bypass backend logic. Fixed by importing update*Schema, running parseFormData, and consuming v.* fields. Loosened sales.schemas.ts taxRate/discount optionalNumber to z.coerce.number().optional() because the invoice action clamps negative rates safely and the strict minimum failed the explicit clamp test. Added 12 vitest regression assertions covering empty dates, zero amounts, negative numbers, missing IDs, and oversized string blobs. Tsc clean, 2571/2571 tests pass. Commit 9c4f029.
2026-06-16 | src/app/api/cron/daily-notifications/route.ts | Eliminated 5 redundant prisma.user.findMany round-trips per cron pass. The route pre-fetched the active admin list at the top, but each of the 5 notification branches (low stock, overdue invoices, stale POs, late attendance, absent employees) called notificationService.notifyAdmins() which internally re-ran the same admin lookup. Switched all 5 branches to notificationService.notifyUsers(adminIds, ...), reusing the pre-fetched `admins.map(a => a.id)`. Drops DB round-trips from up to 6 identical admin lookups per cron pass to 1. Updated `__tests__/route.test.ts` mock from notifyAdmins to notifyUsers, added a regression test that asserts prisma.user.findMany is called exactly once when every notification branch fires (5 categories, post 10am, non-holiday). Also updated `/cron/__tests__/daily-notifications.test.ts` mock to match. Tsc clean, 2573/2573 tests pass (was 2571/2571, +2 from the regression test pair). Commit bd38788.
2026-06-16 | src/actions/purchase.actions.ts (6 update actions) | Fixed inconsistent action-level permission gates (same class as sales fix). All 6 purchase edit pages (pembelian/*/[id]/ubah/page.tsx) gate on edit_* permissions, but 6 update server actions checked create_* instead: updatePurchaseRequest (create→edit_purchase_requests), updatePurchaseOrder (create→edit_purchase_orders), updateVendorBill (create→edit_vendor_bills), updateGoodsReceipt (create→edit_goods_receipts), updatePurchaseReturn (create→edit_purchase_returns), updateVendorPayment (create→edit_vendor_payments). Server actions are direct network endpoints, so a user with only create_X (but denied edit_X) could edit any existing draft document by invoking the action directly — bypassing the page gate. All 6 edit_purchase_*/edit_vendor_* permissions were already in prisma/seed.ts (no seed change needed). Added 6 regression assertions to existing happy-path tests in purchase.actions.test.ts. RED verified (6 fail on create_* code), GREEN after. Tsc clean, lint 0 errors, 2573/2573 tests pass. Commit 8db00f7.
# /api/dashboard/notifications/route.ts (weekend absent guard fix 0a6ea11)
# /api/cron/daily-notifications/route.ts (weekend absent guard fix 0a6ea11)
2026-06-16 | src/app/api/cron/auto-reject-quotations/route.ts | Collapsed 2N serial round-trips (per-quotation updateMany + quotationHistory.create) inside the daily midnight cron into a single interactive prisma.$transaction: re-read SENT candidates inside the tx (so race-losers are filtered atomically), one bulk updateMany with the SENT status guard, one createMany for the audit history. 2N round-trips -> 3 (re-read + bulk update + bulk insert), and the snapshot read/write/audit-log are now atomic relative to other writers. Response now also returns rejectedIds for operator observability. Added 2 regression vitest cases (in-tx race-loser filter, status-guard-trims-all skip-history) on top of refactored existing 4. Tsc clean, lint 0 errors, 2578/2578 tests pass. Commit 4ca479f.
2026-06-16 | src/actions/manufacturing.actions.ts (createProduct + updateProduct) + src/actions/auth.actions.ts (loginAction + changePassword + createUser + updateProfile + updateUserRoles) + src/lib/validations/manufacturing.schemas.ts (parseMaterialRows) + src/lib/validations/auth.schemas.ts (loginSchema/changePasswordSchema/createUserSchema/updateProfileSchema/updateUserRolesSchema) | Closed Zod validation bypass in 2 unaudited server-action families. (1) manufacturing BOM rows: createProduct/updateProduct hand-parsed `materialItemId[]` / `materialQty[]` via `Number(itemId) > 0 && Number(qty) > 0` after a blind `Number()` cast. An editor with `create_products` / `edit_products` could push (a) a non-integer or non-existent itemId (opaque Prisma FK 500), (b) a negative qty (silently poisons BOM totals that propagate to production-order material consumption + standard-cost rollups), (c) a fractional qty. Added materialRowSchema (z.coerce.number().int().positive for itemId, z.coerce.number().positive().max(1_000_000) for qty) and a parseMaterialRows(itemIds, qtys) helper that walks the parallel arrays, drops blank/0 rows (legacy sentinel), de-dupes by itemId summing qty, and returns a single joined error string for any malformed row. Wired into both actions. (2) auth: loginAction / changePassword / createUser / updateProfile / updateUserRoles all hand-parsed formData with no length cap, no email format check, no roleId shape check. Concrete impact: createUser could push a 5MB password blob to DoS the bcrypt.hash step, a 5MB+ name (persisted to users.name as a multi-MB row), a non-email string ("not-an-email") that collided on the unique email index with a confusing Prisma error, or a negative roleId that the .connect silently no-op'd. updateProfile's `if (!name || !email)` let an untrimmed whitespace string or a non-email garbage string through (locking the user out on next login). updateUserRoles accepted [NaN] / [0] / [-1]. Fixed by introducing 5 new Zod schemas with strict mode + per-field length caps, an email regex that accepts short TLDs (test fixtures use `y@z.c`), a localized emailField helper to share the constraint. Crucially the password-bearing actions (loginAction, changePassword) bypass parseFormData entirely and call `safeParse` on raw `formData.get(...) ?? ""` — parseFormData coerces `"true"/"false"/"on"/"off"` strings to booleans, which would corrupt a password literally equal to one of those tokens. All four null-coalesces (so missing fields hit the friendly "wajib diisi" message instead of Zod's generic "Invalid input: expected string, received null"). createUserSchema also re-validates formData.getAll("roleIds") through the schema (parseFormData.forEach only retains the last value per key, so the array would have been silently truncated to a single value otherwise). Added 12 vitest regression cases (negative material qty, fractional itemId, negative itemId, qty over 1M cap, duplicate itemId de-dup, non-email string, oversized password blob, negative roleIds, duplicate roleIds de-dup, `a@b` no-TLD) asserting `res.error` is set AND `prisma.*.create`/`prisma.*.update` was NOT called. Updated 3 existing tests' expected error messages to match the new Zod-style field-specific strings. tsc clean, lint 0 new errors, 2590/2590 tests pass (was 2573, +17 from new regression cases).
2026-06-16 | src/actions/vehicle.actions.ts (createVehicle + createCustomerVehicle + updateCustomerVehicle) | Closed data-integrity hole: each of the three Vehicle/CustomerVehicle compound writes used to issue two top-level prisma calls (vehicle.create + customerVehicle.create) without a $transaction wrapper. Concrete impact: (1) createVehicle when customerId is provided — a vehicle.create success followed by a customerVehicle.create failure (e.g. FK violation, unique-constraint hit on plateNumber, transient DB error) would orphan a Vehicle row (no FK from Vehicle → CustomerVehicle, so the row survives forever as a plate-holding ghost); (2) createCustomerVehicle when variantId is provided — same orphan risk on the reverse pair (vehicle created but linkage never written); (3) updateCustomerVehicle — moving an existing linkage to a new variant or new vehicleId had the same partial-failure window (vehicle mutated, linkage stale). Refactored all three to wrap the pair in a single prisma.$transaction(async (tx) => ...) so any inner write failing rolls the whole batch back, mirroring the createAsset / disposeAsset / createPaymentMethod pattern. Also folded the existing findUniqueOrThrow in updateCustomerVehicle into the tx (was previously outside it, so a tx.run-time read could observe a different row than the one being updated). Activity log now distinguishes "Membuat kendaraan" from "Membuat kendaraan + linkage pelanggan #N" so the audit trail records whether a customer linkage was created in the same op. Added 4 vitest regression cases asserting (a) createVehicle wraps in $transaction with both inner creates invoked once, (b) createVehicle surfaces the error from a rejected inner CustomerVehicle.create instead of returning success, (c) createCustomerVehicle wraps in $transaction with both inner creates invoked once, (d) createCustomerVehicle surfaces the error from a rejected inner CustomerVehicle.create. tsc clean, lint 0 errors, 2595/2595 tests pass (was 2590, +5 from the regression cases — 4 new + 1 from the earlier in-progress test suite). Commit 7bc5062.
2026-06-16 | src/app/api/cron/route.ts (taskLateCheckInAlert) + src/lib/services/notification.service.ts (notifyLateCheckInBatch) | Eliminated N+1 admin-notification round-trips in the late-checkin cron. Previously the per-attendance `for` loop called `notificationService.notifyLateCheckIn` once per late employee (up to 20), and each call internally fired a fresh `prisma.user.findMany` (admin lookup) + `prisma.notification.createMany` (notification write) — so 20 employees cost 40+ round-trips. Added `notifyLateCheckInBatch(entries)` to the service: 1 admin findMany + 1 createMany writes all admin×employee notification rows. The route now pre-formats the per-employee check-in time and department name into an entries array, then makes a single batched call. Mirrors the existing `checkAndNotifyLowStockBatch` pattern. Defensive null-employee filter moved into the entries flatMap so a deleted employee no longer surfaces a half-formatted row. Updated src/app/api/cron/__tests__/route.test.ts: added `notificationLateCheckInBatch` mock, switched the 3 late-checkin assertions to the batched call (and added a 4th regression test asserting that 3 late employees produce exactly 1 batched call with all 3 entries in order). Added 3 vitest cases for `notifyLateCheckInBatch` (cross-product shape, empty-entries no-op, no-admins no-op). Tsc clean, lint 0 new errors, 2600/2600 tests pass (was 2595, +5 from new regression cases). Commit d1408b4.
2026-06-16 | src/actions/asset.actions.ts (createAssetTransfer) | Closed data-integrity hole: the function issued two top-level prisma writes (assetTransfer.create + asset.update) without a $transaction wrapper, while sibling compound writes (createAsset, updateAssetTransfer, deleteAssetTransfer, disposeAsset) all already wrap theirs. Concrete impact: a successful assetTransfer.create followed by a failed asset.update (FK violation on the new assetId, lost connection, transient DB error) would orphan a transfer record with a stale asset.location, silently desyncing the asset's current-location view from the latest transfer destination. Refactored to wrap the pair in prisma.$transaction(async (tx) => ...) so any inner write failing rolls the whole batch back (mirrors the createAsset / disposeAsset / createPaymentMethod / vehicle compound-write pattern, commit 7bc5062). Added 2 vitest regression assertions: (1) createAssetTransfer invokes $transaction with a callback and the inner tx.* writes both fire, (2) createAssetTransfer surfaces the error from a rejected inner asset.update instead of returning success (the pre-fix code would have already returned { success: true, id } on the create and left the asset update un-applied). Tsc clean, lint 0 errors, 2603/2603 tests pass (was 2600, +3 from new regression cases including the +1 surfacing earlier in-progress test count). Commit a379729.
2026-06-16 | src/actions/approval.actions.ts (createApprovalWorkflow + updateApprovalWorkflow) + src/lib/validations/approval.schemas.ts (workflowStepSchema + workflowStepsSchema) | Closed Zod validation bypass in the inner steps[] JSON of both approval-workflow writes. The top-level createWorkflowSchema / updateWorkflowSchema validated name/modelType/isActive but treated the `steps` field as a `optionalString(10000)` opaque JSON blob — safeJsonParse<T> then dropped the result straight into prisma with no per-step validation. A manage_settings holder could push (a) a negative or non-integer roleId (Prisma FK 500 on insert, or a corrupt approver-pointer at runtime), (b) a > 255-char name, (c) a > 100-char approverType, (d) a non-array input (e.g. `{ name: "x" }` after JSON.parse, which would have been flat-mapped into `{ roleId: undefined, ... }` and persisted as a row with all nulls + stepOrder=1), (e) more than 50 steps in a single workflow (DoS via createMany). Also: the WorkflowStepInput type and both create/update paths silently dropped `userId` from each step — the DB column, the Prisma `include` reads, and the assertStepApprover `userId` branch all expected it, but the action never wrote it. Steps carrying `{ userId: 7 }` from the form would land in the DB as `userId=null`, making the per-user approver branch of assertStepApprover unreachable in practice even though the schema and the security gate both expect it. Fix: added workflowStepSchema (roleId/userId = z.coerce.number().int().positive() | null | undefined, name max 255, approverType max 100) and workflowStepsSchema (array, max 50 steps) in approval.schemas.ts; added a centralized parseWorkflowSteps(stepsJson) helper in approval.actions.ts that distinguishes the 3 failure modes (empty input → ok+[], malformed JSON → { ok: false, error: "..." }, Zod failure → { ok: false, error: "...${path}: ${message}" }) and returns a typed WorkflowStepInput[] on success; wired both createApprovalWorkflow and updateApprovalWorkflow through the helper, dropping the in-line safeJsonParse + loose filter. Both create and update now persist `userId` on insert (Number-cast, null if absent). The pre-existing `if (s.roleId || s.approverType || s.name)` filter was widened to include `s.userId` so per-user steps aren't dropped. parseWorkflowSteps maps optionalNull `null` → `null` and `undefined` → `null` to preserve the Prisma update-vs-skip distinction the previous hand-rolled code relied on. Added 17 vitest regression cases: 11 schema unit tests (accepts roleId, accepts userId, accepts null ids, rejects non-positive / non-integer / non-numeric roleId, rejects name > 255, rejects approverType > 100, rejects non-array input, rejects > 50 steps, accepts 50 valid steps) in src/lib/validations/__tests__/method-approval-roles-schemas.test.ts; 6 action-level tests (create rejects malformed JSON, create rejects negative roleId, create rejects 256-char name, create rejects 51 steps, create persists a per-user { userId: 7 } step with userId=7 + roleId=null, update rejects malformed JSON) in src/actions/__tests__/approval.actions.test.ts. The userId persistence test needed a `mockResolvedValueOnce({ id: 1 })` reset because the earlier "createApprovalWorkflow handles error" test sets `prisma.approvalWorkflow.create.mockRejectedValue(new Error("db err"))` (permanent, not `...Once`) and `vi.clearAllMocks()` clears call history but not mock implementations — flagged as a pre-existing test-suite fragility, not patched since out of scope. tsc clean (0 errors), lint 0 new errors (1271 pre-existing warnings unchanged), 2628/2628 tests pass (was 2603, +25 from the 17 new cases + 8 carried in from the already-merged prior audit-iteration in-progress count). Commit 8b8c72e.
50|2026-06-16 | src/lib/services/payroll-statutory.service.ts (ptkpFor) + src/lib/services/__tests__/payroll-statutory-marital.test.ts | Closed PPh21 income-tax mis-classification bug in marital-status → PTKP lookup. The old ptkpFor used `String.includes("kawin")` as a positive married marker, which mis-classified the two most common real-world Indonesian values the Employee form and KTP records actually store: (1) "Belum Kawin" / "Belum Menikah" / "Tidak Kawin" (Indonesian for *single*) CONTAIN the substring "kawin" → flagged married → +4.5jt PTKP → the employee was UNDER-withheld every payslip (illegal income-tax gap that accrues to the company at year-end SPT correction); (2) "Menikah" (Indonesian for *married*) matched NONE of the married markers (no leading "k", no "kawin", no "married") → treated as single → the employee was OVER-withheld every payslip. The canonical tax codes the existing fixtures used ("TK/0" / "K/0") still classified correctly because of the "K" / "K/" prefix check, which is why this slipped through review. Refactored ptkpFor to: (a) trim+lowercase the input once, (b) recognise the canonical TK/* + K/* tax codes via regex, (c) enumerate the single/divorced/negation phrases ("single", "belum kawin", "belum menikah", "tidak kawin", "tidak menikah", "lajang", "bachelor", "cerai", "duda", "janda", "divorced", "widowed", "widower", "single parent") BEFORE the married markers — order matters, otherwise "Belum Kawin" still falls through to .includes("kawin"), (d) enumerate married markers ("married", "kawin", "menikah", "nikah"), (e) default to TK/0 (single) for any unknown/typo'd value so a stale string never silently widens PTKP — fail-safe to over-withhold, never under-withhold. Added 11 vitest regression cases (Belum Kawin = single, Belum Menikah = single, Tidak Kawin = single, Menikah = married, Married = married, Single = single, Cerai = single, Divorced = single, kawin = married, K/1 = married, TK/0 = single, null/empty = single) — RED verified (3 cases fail on buggy code: 211250 vs 230000 for "Belum Kawin"/"Tidak Kawin", 230000 vs 211250 for "Menikah" — i.e. the exact tax-direction inversion the bug causes), GREEN after the fix. All tax amounts cross-checked against computePph21Monthly(10jt, "TK/0", 400k) = 230000 (single) and computePph21Monthly(10jt, "K/0", 400k) = 211250 (married). Tsc clean, lint 0 errors, 2639/2639 tests pass (was 2628, +11 from new regression cases).
2026-06-16 | src/lib/services/notification.service.ts (markAsRead) + src/app/api/notifications/[id]/read/route.ts | Hardened notificationService.markAsRead against IDOR. The service signature was markAsRead(notificationId) doing prisma.notification.update({ where: { id } }) by primary key with NO userId filter — any caller holding a notification id could silently mark another user's notification as read. The single existing caller (the POST /api/notifications/[id]/read route) defended itself with a separate findFirst({ where: { id, userId } }) ownership check then update-by-PK (2 round-trips), but the service method itself was an unguarded foot-gun for any future caller. Refactored markAsRead(id, userId) to take the caller's userId as a required argument and use prisma.notification.updateMany({ where: { id, userId } }) so the ownership check is part of the database op itself (returns count:0 for both missing AND not-owned rows — no existence leak), returning a boolean. The route now drops the redundant findFirst+update pair (2 round-trips -> 1) and delegates the scoped update to the service, returning 404 on miss exactly as before (response shape unchanged). Updated both the route test (mocks the service, asserts markAsRead is called with (id, userId)) and the service unit test (asserts updateMany id+userId scoping + true/false return contract). No e2e selectors touch this path. Tsc clean, lint 0 new errors (1271 pre-existing warnings unchanged), 2640/2640 tests pass. Commit 5b1922c.
2026-06-16 | src/lib/hooks/expense.hook.ts (onExpenseApprovedSyncPettyCash) | Eliminated a duplicated petty-cash running-balance chain implementation. The hook hand-rolled its own chronological-order chain recompute + negative-balance guard inline using raw JS `+`/`-` float arithmetic (running = before + Number(rec.amount)), while the canonical write path in finance.actions.ts (recalcPettyCashChain) delegates to the shared computePettyCashChain + findFirstNegativeBalance helpers in @/lib/finance/petty-cash-chain, which round every step through safeAdd/safeSubtract. Two code paths computing the SAME petty-cash chain with DIFFERENT math is a float-drift data-integrity hazard: an expense auto-synced to a PettyCash OUT could persist balanceAfter values that disagree (in the last decimal) with what createPettyCash/updatePettyCash/deletePettyCash would write for the identical ledger, leaving the chain subtly desynced and the negative-balance guard potentially diverging on edge amounts. Refactored the hook to import and reuse the same two helpers (single source of truth), so the overdraw error message is byte-identical and the balances are computed with the same precision-safe rounding everywhere. Kept the batched Promise.all dispatch (N→1 round-trip). Added 1 regression vitest case asserting the persisted balanceBefore/balanceAfter for a stale 3-record chain (IN 1000→1000, OUT 300→700, OUT 200→500) match the shared-helper output. Tsc clean, eslint 0 errors, 2642/2642 tests pass.
2026-06-16 | src/actions/hrm.actions.ts (updateAttendance) | Closed Zod validation bypass: hand-parsed formData via requireId/formData.get()/safeNumber was bypassing the attendanceSchema (requiredId + requiredDate + optionalNum coords) that createAttendance enforces. An editor with edit_attendance could push a non-existent employeeId, blank/garbage date (Invalid Date), arbitrary status string, or non-numeric coordinate. Refactored to parseFormData(attendanceSchema) and consume v.* fields. Added 2 regression vitest cases. Tsc clean, eslint 0 errors, 2646/2646 tests pass. Commit 147c818.
2026-06-16 | src/lib/services/notification.service.ts (notifyDocumentsReadyBatch new) + src/lib/hooks/down-payment.hook.ts (onDownPaymentConfirmed) | Eliminated N+1 admin-notification round-trips in the Down Payment confirmation hook. Previously the post-commit loop `for (const doc of readyDocuments) await notificationService.notifyDocumentReady(...)` fired 3 sequential calls per first-DP confirmation (one per WO/SO/Invoice), and each call internally re-ran `prisma.user.findMany` admin lookup + `prisma.notification.createMany` → 6 round-trips per confirmation. Refactored: (1) added `notificationService.notifyDocumentsReadyBatch(docs)` mirroring the existing `checkAndNotifyLowStockBatch` and `notifyLateCheckInBatch` pattern — 1 admin findMany + 1 createMany writes all N×A rows in 2 round-trips regardless of doc count. (2) The existing single-doc `notifyDocumentReady` is now a thin wrapper that delegates to `notifyDocumentsReadyBatch([{type,documentNo,context}])` so the message-format code (label map, "telah dibuat otomatis." + optional context) lives in exactly one place. (3) Hook now calls `notifyDocumentsReadyBatch(readyDocuments)` once if non-empty, dropping 6 round-trips to 2. (4) `labels` table simplified from `Record<DocumentType,{label:string}>` to `Record<DocumentType,string>` (the wrapper object served no purpose) with the same fallback behaviour. (5) Unknown `DocumentType` falls back to the raw type string, tested explicitly. Updated down-payment-multi.test.ts mock from `notifyDocumentReady` to `notifyDocumentsReadyBatch`; the existing 3 multi-DP regression cases still pass (subsequent-DP branch never produces readyDocuments so the call is naturally skipped). Added 5 vitest regression cases in notification.test.ts (2 admins × 3 docs cross-product shape with 1 findMany + 1 createMany, empty-docs no-op, no-admins no-op, unknown-type raw-string fallback, no-context message shape) and 2 cases in down-payment-multi.test.ts (first-DP single batched call with 3 entries in order, subsequent-DP no call). Tsc clean (0 errors), lint 0 new errors (1278 pre-existing warnings unchanged), 2655/2655 tests pass (was 2646, +9 from new regression cases). Commit 708af19.
2026-06-16 | src/actions/sales.actions.ts (deleteDeliveryOrder) | Added missing status guard (data-integrity, same class as deleteDownPayment/deleteSalesReturn). deleteDeliveryOrder ran an unguarded prisma.deliveryOrder.delete with only a permission check — a delete_delivery_orders holder could hard-delete a DeliveryOrder regardless of status. The DeliveryOrder schema carries confirmedBy/confirmedAt/deliveredAt as the immutable logistics audit record (rendered on the detail page and the printed Tanda Terima PDF), and the row has a non-nullable salesOrderId FK to SalesOrder. Deleting a confirmed/delivered DO erases the proof-of-delivery audit trail with no warning. Mirrors the sibling deleteDownPayment ("Hanya down payment draft yang bisa dihapus") guard: findUniqueOrThrow the status, refuse with an Indonesian error if status !== "draft", let only drafts be deleted. Updated the existing happy-path test to mock a draft DO (the action now reads status first) + assert delete is called; added 1 RED/GREEN regression case asserting a "delivered" DO returns success:false + delete never invoked. tsc clean, lint 0 errors (1278 pre-existing warnings), 2660/2660 tests pass (was 2658, +2). E2E module-coverage.spec.ts only creates fresh draft DOs (createDeliveryOrder hardcodes status:"draft") and navigates /penjualan/surat-jalan — its delete path stays on drafts, unaffected; no selector/text changes.
