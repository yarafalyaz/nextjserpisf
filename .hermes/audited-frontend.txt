320 dashboard page routes audited (loading+error coverage), src/app/(dashboard)/*/error.tsx
src/app/error.tsx (removed invalid <html><body> wrapper from route-level error boundary, removed broken custom-Button onPress usage, added role="alert" + aria-live)
src/app/(dashboard)/not-found.tsx (fixed broken 404 numeral class: text-center-code → text-6xl font-bold)
src/app/(auth)/kebijakan-privasi/page.tsx (added aria-label + focus ring to back link)
src/app/(auth)/ketentuan-layanan/page.tsx (added aria-label + focus ring to back link)
335 module error boundaries (src/app/(dashboard)/**/error.tsx) hardened: added role='alert', aria-live='assertive', useEffect console.error, standard Shadcn Button and lucide-react AlertTriangle icons for accessibility.
src/components/ui/{form-layout,detail-table,page-layout,detail-layout}.tsx (removed unnecessary 'use client' directive to leverage React Server Components)
src/components/attendance/self-attendance-widget.tsx (replaced broken UnoCSS i-lucide-alert-triangle with lucide-react AlertTriangle, added role='alert'+aria-live, extracted getStatusLabel/getStatusBadgeClass for present/late/half_day/absent, added region role+aria-label to widget shell)
src/app/(auth)/error.tsx (added role='alert'+aria-live='assertive', focus ring on reset button)
src/app/(auth)/not-found.tsx (added aria-label + focus-visible ring on back-to-login button)
src/app/globals.css (added missing --color-info and --color-info-soft/-foreground/-600 tokens; fixed broken text-info/bg-info utilities used by notifications-widget)
src/components/ui/status-chip.tsx (removed unnecessary 'use client' — no hooks/state/handlers; promotes component to React Server Component for smaller client bundle)
src/components/ui/breadcrumbs.tsx (removed unnecessary 'use client' — pure presentational (Link + icon); now renders fully on the server)
src/components/ui/search-field.tsx (a11y: added role='search' + aria-label to <form>, aria-label to <input>, and aria-hidden='true' to decorative search icon — landmark discovery + accessible name for screen readers)
src/components/reports/date-presets.tsx (a11y: added role='group'+aria-label='Rentang waktu cepat' wrapper, type='button' to prevent form-submit, aria-label per preset for accessible name, focus-visible ring)
src/components/reports/export-buttons.tsx (a11y: added role='group'+aria-label='Export dokumen' wrapper, type='button', aria-label per action, aria-hidden on decorative lucide icons, focus-visible ring)
src/components/reports/report-date-filter.tsx (a11y: role='search'+aria-label on both date-range and single-date filter containers, aria-label on Generate button)
src/components/ui/detail-tabs.tsx (responsive: wrapped TabsList in overflow-x-auto container with justify-start on mobile / justify-center on sm+; focus-visible ring on TabsContent for keyboard a11y)
src/components/forms/vendor-bill-form.tsx (fix: date/dueDate state hydration on edit overriding stored DB values and GL posting date with today/null)
src/components/ui/page-header.tsx (promoted to Server Component by extracting interactive Button to its own client module)
22|src/components/activity-log/stats-cards.tsx (a11y: added role='region'+aria-label on grid, role='group'+composite aria-label on each card, aria-hidden on inner text to prevent double announcements)
23|src/components/ui/empty-state.tsx (a11y: collapsed StatCard nested aria-labels into single composite aria-label on the role='group' wrapper; aria-hidden inner text to avoid SR double-reads)
24|src/components/ui/qr-code-display.tsx (a11y: added role='img'+aria-label='Kode QR ...' on wrapper, aria-hidden+focusable='false' on raw <QRCodeSVG/> so screen readers announce a single coherent label)
25|src/components/ui/action-dropdown.tsx (a11y: aria-label='Buka menu aksi' on trigger, aria-hidden='true' on all decorative lucide icons inside DropdownMenuItem)
src/components/ui/filter-drawer.tsx (added aria-label to open/close buttons, aria-hidden to decorative icon)
src/components/ui/form-section.tsx (promoted div to semantic section, added aria-labelledby linking to a unique generated id on the section header)

26|src/components/ui/empty-state.tsx (formatted for visual a11y/clarity)
27|src/components/ui/skeletons.tsx (a11y: added role=status+aria-label+Memuat ...+aria-busy=true to TableSkeleton/FormSkeleton/DetailSkeleton/DashboardSkeleton/ReportSkeleton/SimplePageSkeleton)
28|src/components/dashboard/section-cards.tsx (promoted to RSC by removing unnecessary use client; pure presentational, no hooks/handlers)
29|src/components/dashboard/recent-invoices-table.tsx (a11y: aria-label to search input + column dropdown, aria-label to pagination buttons, aria-hidden on decorative search/sliders/chevron icons)
30|src/components/ui/print-button.tsx (import: switch from @/components/ui/page-header to @/components/ui/button for direct dependency)
31|src/components/ui/status-actions.tsx (import: switch from @/components/ui/page-header to @/components/ui/button for direct dependency)
32|src/components/layout/notification-dropdown.tsx (a11y: aria-haspopup=menu, aria-label on unread badge, aria-hidden on bell icon; import: switch from page-header to button)
33|src/app/(dashboard)/aset/{_components/asset-table.tsx,[id]/_components/dispose-asset-button.tsx,[id]/page.tsx,loading.tsx} (a11y: enriched aria-label on action link, added aria-labelledby+id on dispose currency input, removed fake cursor-pointer on history badge, added role=status+aria-busy on loading skeleton)
src/components/ui/currency-input.tsx (fix: controlled CurrencyInput was uneditable for decimals/clearing — visibleValue was bound to formatDisplay(controlledValue) instead of local state, so every keystroke round-tripped through onChange→parent→reformat. Typing a decimal comma (2.500,) was instantly stripped back to 2.500, a trailing zero (2.500,50) collapsed to 2.500,5, and deleting the last digit jumped back to "0" — making decimal entry and field-clearing impossible across every money field in the app (payroll, vendor bills, journals, expenses, invoice items, item cost/price, etc.). Now the <input> binds to local displayValue and a render-phase state-sync (prevControlledValue guard) only overwrites it when the parent's value is numerically different from what's typed, preserving in-progress decimals while still resyncing on external/reset changes. +8 jsdom tests via react-dom/client. tsc + lint clean, 2536/2536 tests pass.)
src/components/{nav-documents,nav-main,nav-secondary,nav-user,app-sidebar,site-header}.tsx (REMOVED: orphaned shadcn dashboard-template leftovers — nav-documents/main/secondary had zero importers; nav-user.tsx duplicated the real session-aware @/components/layout/nav-user; app-sidebar.tsx + site-header.tsx were thin re-export/wrapper shims. Rewired src/app/(dashboard)/layout.tsx to import AppSidebar from @/components/layout/sidebar and Header from @/components/layout/header directly, removing a layer of indirection. -317 LOC; tsc clean, lint 0 errors.)
34|src/components/ui/data-table.tsx (a11y: added aria-sort=ascending/descending/none to sortable <TableHead> so screen readers announce current sort state; marked ChevronUp/ChevronDown/ArrowUpDown sort indicators aria-hidden='true' as decorative)
35|src/components/ui/detail-layout.tsx (semantic HTML: DetailGrid now renders <dl> and DetailItem renders <dt>/<dd> instead of generic div/span/span — key/value detail pairs are now a proper description list for assistive tech)
41|src/components/ui/detail-card.tsx (a11y: refactored DetailCard to render inner grid as semantic <dl>, switched DetailField to <dt>/<dd> for term/description semantics, converted outer wrapper to <section> with aria-labelledby for landmark nav, DetailSection likewise promoted to <section>+aria-labelledby; responsive grid + col-span layout preserved across 50+ detail pages)
42|src/components/ui/address-picker.tsx (a11y: each <Label> now has htmlFor tied to its Combobox via a derived idPrefix (empty when no `prefix` prop, `prefix-` otherwise) so screen readers announce the correct field name when focus enters the combobox/search input; postal-code <Input> also got matching id)
src/components/ui/invoice-items-editor.tsx (accessibility: added scope='col' to table headers, sr-only labels for inputs/textareas/selects, explicit aria-label on delete buttons, and added vitest coverage)
43|src/components/ui/page-header.tsx + 153 importers (removed 'export { Button }' re-export; all consumers now import Button directly from @/components/ui/button — eliminates indirection and tightens dependency graph; tsc clean, lint 0 errors)
44|src/components/dashboard/notifications-widget.tsx (a11y: loading state gets role=status+aria-live=polite+aria-busy=true; alert grid wrapped in role=region+aria-label; each alert <Link> gets composite aria-label 'Label: count', focus-visible ring, decorative icon/badges aria-hidden; notifikasi/aktivitas sections promoted to role=region+aria-label; decorative Bell/Clock/Check <lucide> icons hidden from AT)
45|src/components/ui/combobox.tsx, src/components/ui/multi-combobox.tsx (a11y: decorative <Check>/<X>/<ChevronsUpDown> lucide icons get explicit aria-hidden=true so screen readers don't announce glyphs alongside the option label)
46|src/components/ui/date-picker.tsx (a11y: <CalendarIcon> inside the trigger is decorative — the formatted date is the accessible name; icon hidden from AT)
47|src/components/ui/{delete-button,void-button}.tsx (a11y: <Trash2>/<Ban> icons inside the buttons hidden from AT since the visible label / aria-label='Hapus' is the accessible name)
48|src/components/ui/theme-toggle.tsx (a11y: <Sun>/<Moon>/<Icon>/<Check> lucide icons hidden from AT — the trigger has aria-label='Ganti tema' and the items have visible text labels)
49|src/components/ui/status-actions.tsx (a11y: status chip <Clock>/<CheckCircle>/<XCircle> and approve/reject button <CheckCircle>/<XCircle> icons hidden from AT)
50|src/components/layout/header.tsx (a11y: decorative <Search>/<User>/<Bell>/<Settings>/<LogOut> lucide icons hidden from AT — the trigger buttons have aria-label or visible text, dropdown items have text labels)
51|src/components/layout/nav-user.tsx (a11y: <ChevronsUpDown> trigger icon, and <User>/<LogOut> icons in dropdown items hidden from AT)
52|src/components/login-form.tsx (a11y: error <svg> alert circle and Google <svg> hidden from AT — the visible text is the accessible name)
53|src/components/ui/shadcn/sidebar.tsx (a11y: <PanelLeftIcon> in SidebarTrigger hidden from AT — the sr-only 'Toggle Sidebar' span is the accessible name)
54|src/components/ui/combobox.tsx, src/components/ui/form-select.tsx (a11y: FormSelect declared an `aria-label` prop in its public API (10+ consumers) but dropped it on the floor — the underlying Combobox didn't even support aria-label, so the field's accessible name defaulted to its visible label/placeholder which broke for icon-only/placeholder-less dropdowns. Added `"aria-label"?: string` to ComboboxProps, bound it to aria-label on the <input role="combobox">, and threaded it through FormSelect so the prop is actually honored end-to-end. tsc clean, lint 0 errors.)
55|src/components/ui/transaction-attachments.tsx (a11y+semantic: promoted wrapper div→<section> with aria-labelledby tied to the heading; attachment grid div→<ul>/<li> proper list semantics (added list-style:none/margin:0/padding:0 reset in globals.css .attachment-grid); added role=status+aria-live=polite+aria-busy sr-only region announcing load state and attachment count; image SafeImage alt=""/role=presentation since the wrapping <a> now carries a composite aria-label ("Buka pratinjau gambar/PDF <name> di tab baru"); download link + delete Button got descriptive aria-labels including filename; file <input> got aria-label + sr-only(was hidden)+tabIndex=-1 + aria-controls from trigger; all decorative lucide icons (FileText/Upload/Download/Trash2) marked aria-hidden; file-size span got aria-label. tsc clean, lint 0 errors.)
56|src/components/ui/form-attachment-upload.tsx + src/components/forms/vendor-payment-form.tsx (a11y+semantic: promoted wrapper div→<section aria-labelledby> tied to the field <Label>; attachment list div→<ul>/<li> proper list semantics (added list-style:none/margin:0/padding:0 reset to .form-attachment-list in globals.css); added role=status+aria-live=polite+aria-busy sr-only region announcing upload-in-progress; thumbnail SafeImage/Image alt="" + icon wrapper aria-hidden since the visible filename is the accessible name; remove Button got descriptive aria-label "Hapus lampiran <name>" + isIconOnly/danger-soft variant; decorative Upload/X/FileText lucide icons marked aria-hidden; file <input> got aria-label + sr-only(was class=hidden)+tabIndex=-1 + aria-controls wired to the upload trigger; file-size span got aria-label. No e2e selectors referenced these components. tsc clean, lint 0 errors, 2578/2578 tests pass.)
57|src/components/ui/time-picker.tsx (a11y: wrapper got role=group+aria-label='Pemilih waktu' for landmark grouping; inner hour/minute Comboboxes got explicit aria-label='Jam'/'Menit' so screen readers announce the field's purpose independent of placeholder text; minute Combobox got id='{id}-menit' to avoid duplicate-id collisions with the hour input; decorative Clock lucide icon and the ':' separator between fields got explicit aria-hidden='true')
58|src/components/layout/command-palette.tsx (a11y: lucide menu icons inside CommandItem marked aria-hidden=true — every item already has a visible text label, so the glyph is purely decorative and was being announced alongside the label by screen readers)
59|src/components/ui/breadcrumbs.tsx (a11y: parametrised nav aria-label (default "Navigasi breadcrumb" — was hardcoded English "Breadcrumbs" which is inconsistent with the rest of the Indonesian UI); explicit aria-hidden="true" on the ChevronRight separator instead of bare aria-hidden boolean; ariaLabel prop is optional and backward-compatible — 0 e2e/spec changes needed, tsc + lint clean)
60|src/components/ui/shadcn/command.tsx (a11y: SearchIcon in CommandInput now aria-hidden="true" — the placeholder is the accessible name, so the magnifier glyph was being announced redundantly by screen readers; affects all CommandDialog consumers including the global command-palette)
61|src/components/dashboard/charts.tsx (a11y: chart-as-image semantics — wrapped RevenueChart/SalesStatusChart/ProjectPipelineChart <ChartContainer>s in role="img" with aria-labelledby pointing to <CardTitle id=...> + aria-describedby to sr-only span summarising the data; added sr-only <table> with caption/scope headers so screen-reader users get the full underlying data series (date→tagihan/lunas for revenue, status→count+percentage for pie, stage→count for pipeline); added aria-label="Pilih rentang waktu" to the desktop ToggleGroup so it groups 3 bulan/30 hari/7 hari for landmark nav; promoted the 3 empty-state <p>s to role="status" so SR users hear "Belum ada data …" announcements; removed redundant visible legend chips in SalesStatusChart now that the sr-only table replaces them; no e2e selectors referenced these chart elements. tsc + lint clean, 0 new errors.)
62|src/components/activity-log/detail-drawer.tsx (a11y+semantic: meta grid 14 generic div/span→proper <dl>/<dt>/<dd> term-description pairs in aria-label='Metadata log aktivitas' landmark; 'IP'→'Alamat IP' for Indonesian consistency; time wrapped in <time dateTime={row.createdAt}>; action Badge got aria-label='Aksi: <text>'; diff section promoted to <section aria-labelledby=...> with explicit heading id; changed rows now role='group' + aria-label='<key>: berubah dari <old> menjadi <new>'; diff <dl> aria-label='N dari M kolom berubah'; empty-diff fallback promoted to role='status'; '→' separator aria-hidden. No e2e selectors referenced the component. tsc clean, lint 0 errors, 9/9 activity-log unit tests pass.)
63|src/components/dashboard/section-cards.tsx (a11y+semantic: 7 decorative lucide icons — TrendingUpIcon×2, TrendingDownIcon, DollarSign, Receipt, Users×2, Car, CheckCircle2 — now aria-hidden='true' since every card already has a visible CardDescription/CardFooter text label serving as the accessible name; wrapper <div>→<section aria-label='Ringkasan kinerja'> for screen-reader landmark nav. No e2e selectors reference the component, no test coverage needed. tsc clean, lint 0 errors.)
64|src/components/ui/confirm-dialog.tsx (fix+icon+id-wiring: the public-API variant prop was declared as "danger" | "warning" | "accent" | "success" but only "danger" ever got styling — approve-button (penggajian) passes variant="warning" and mark-paid-button passes variant="accent", so both silently fell through to the default shadcn Button look and lost their semantic color. Mapped all 4 variants to a variantConfig table: danger→destructive (red), warning→bg-warning (amber), accent→bg-primary (brand), success→bg-success (emerald) — confirm buttons now visually match their intent so a destructive "Finalisasi" can't be confused with a benign "Tandai Dibayar". Added AlertDialogMedia icon slot per variant (AlertTriangle / TriangleAlert / Info / CheckCircle2, all aria-hidden='true' as decorative glyphs paired with the visible title). Explicit useId() wires <AlertDialogTitle id=…> and <AlertDialogDescription id=…> so screen readers announce both the short label and the longer prompt; Radix's Content auto-binds aria-labelledby/aria-describedby to the id of the AlertDialogTitle/Description slot. Public variant API + body/children/confirmLabel/cancelLabel/disabled surface unchanged — 0 e2e specs needed updating (they target title text "Hapus data ini?" and getByRole("alertdialog", { name: … }) which still resolve). tsc clean, lint 0 errors, 2640/2640 tests pass.)
65|src/components/ui/data-table.tsx (a11y: decorative lucide icons in toolbar/pagination/bulk-bar now aria-hidden — Search magnifier, Kolom ChevronDown, bulk-delete Trash2, and all 4 pagination chevrons (ChevronsLeft/ChevronLeft/ChevronRight/ChevronsRight) were being announced redundantly alongside their visible/sr-only labels; pagination wrapper div→<nav aria-label='Pagination tabel'> landmark; row-count summary + 'Halaman N dari M' indicator got aria-live='polite' so SR users hear page/selection changes; 'Baris per halaman' promoted from <p> to <label htmlFor='pageSize'> wired to the SelectTrigger id+aria-label for an accessible page-size control. No e2e selectors referenced these elements (verified: pagination labels/Kolom/Hapus Terpilih unused in e2e). tsc clean, lint 0 errors, 2642/2642 tests pass.)
66|src/components/login-form.tsx (UX/integrity: removed dead "Masuk dengan Google" button + misleading "akun Google Anda" copy — auth.ts has only CredentialsProvider, no GoogleProvider. Fixed useState lazy-init that mutated window.history during render → useSearchParams() + useEffect for URL cleanup. A11y: aria-live="assertive" on error region, aria-describedby linking inputs to error, aria-busy on form, autoFocus on email, aria-hidden on decorative icons, placeholder on password, error-clears on input. Wrapped in <Suspense> in page for useSearchParams bail-out.)
67|src/app/(auth)/login/page.tsx (wrapped LoginForm in <Suspense> with custom LoginFallback skeleton; added aria-hidden on decorative GalleryVerticalEnd icon; server component preserved.)
68|src/app/(auth)/login/loading.tsx (new — route-level loading boundary with role=status+aria-label+aria-busy skeleton for streaming SSR fallback)
69|src/components/dashboard/recent-invoices-table.tsx (a11y: SortButton got aria-label="Urutkan <kolom>"; ArrowUpDown icon aria-hidden; TableHead now has scope="col"+aria-sort reflecting TanStack column sort state (ascending/descending); paidAmount progress bar got role="progressbar" with aria-valuemin/max/now + aria-valuetext "N persen terbayar" + composite aria-label — SR users hear payment progress instead of a mute div; date cell wrapped in <time dateTime=...>; customer name gets title= for hover-tooltip on truncated text; empty-state cell role="status"; sr-only <caption> on table summarizing columns; pagination wrapped in <nav aria-label="Pagination tabel faktur">; selected-row summary + "Halaman N dari M" both aria-live=polite; ArrowUpRight/Search/SlidersHorizontal/ChevronLeft/Right lucide icons all aria-hidden; Card wrapper aria-labelledby tying to the CardTitle id — no e2e spec references the component)
70|src/components/lazy/dashboard-charts.tsx (a11y: extracted ChartSkeleton helper with role=status+aria-label="Memuat grafik"+aria-busy=true for the 3 nextDynamic chart loaders; previous bare <div> with animate-pulse was announceable as nothing meaningful to AT)
71|src/components/reports/print-header.tsx (a11y+semantic: wrapper div→<header aria-label="Header cetak laporan"> landmark; inner div→flex container with print:flex/print:flex-row/print:items-center/print:justify-between so the header actually displays (was display:none with print:block, which doesn't override tailwind print: utility precedence issues — now using print: utilities consistently); both <h1>/<h2> demoted to <span>/<p> to avoid duplicate-h1 conflict with the on-screen page heading; printed-at date wrapped in <time dateTime={ISO}> + sr-only "Dicetak: " prefix; "Dicetak: " marked aria-hidden since <time> is the accessible name; no e2e spec references the component)
72|src/components/ui/page-layout.tsx (a11y: PageSubtitle promoted from bare <p> to <p role="doc-subtitle"> — ARIA role designed for subtitle text paired with a heading; screen readers that understand it (NVDA/JAWS/VoiceOver) announce the subtitle alongside the preceding h1, so e.g. "Laporan Keuangan, doc-subtitle: Periode 1 Jan – 31 Des 2026" — backward-compatible, no consumer changes)
73|src/components/ui/detail-table.tsx (a11y: DetailTableTh now emits scope="col" by default (the 485+ usages are all column headers in DetailTableHead) — screen readers previously had no way to associate <th> with <td> data cells (WCAG 1.3.1); new optional `scope` prop accepts "col" | "row" | "colgroup" | "rowgroup" for any future row-header use. tsc clean, lint 0 errors, 0 e2e specs reference the component so no selector sync needed.)
2026-06-16 | src/components/ui/form-select.tsx, src/components/ui/combobox.tsx (a11y: required prop wired through) | Closed the "declared in public API, dropped on the floor" bug on FormSelect: `required?: boolean` was in FormSelectProps for 60+ consumers but never destructured and never forwarded to Combobox, so a `required` dropdown silently submitted empty values and never announced required state to screen readers. (1) Added `required?: boolean` to ComboboxProps with a JSDoc explaining the dual-target semantics: the visible search input gets `aria-required={required || undefined}` for AT announcements, and the hidden form-submission `<input type="hidden" name={name}>` gets the native `required` attribute so HTML form validation blocks empty submissions. (2) Threaded `required` through FormSelect → Combobox. (3) Added `aria-haspopup="listbox"` on the combobox input per WAI-ARIA combobox 1.2 pattern (already implied by `aria-controls` + `role="listbox"`, but the explicit token is what screen readers look up first). (4) Kept `aria-required={required || undefined}` (not `aria-required="false"`) so it stays absent when the field is optional — ARIA spec only allows true/blank, not false. PRE-COMMIT GUARD: grep e2e/ for `aria-haspopup`/`aria-required` returns 0 hits; `getByRole("combobox")` and `input[placeholder='Cari ...']` selectors unaffected (role attribute and placeholder text on the input are unchanged); e2e project-crud/sales-crud/inventory-rack-row/finance-journal/hr-attendance all target the search input by role+placeholder, not by `aria-required` or `aria-haspopup`. tsc clean (0 errors), lint 0 errors (1280 pre-existing warnings unchanged).
